navbar.navbar-default.navbar-fixed-top
	div.container-fluid
		div.navbar-header
			button.navbar-toggle.collapsed(type="button", data-toggle="collapse", data-target="#navbar-collapse-1", aria-expanded="false")
				span.sr-only Toggle navigation
				span.icon-bar
				span.icon-bar
				span.icon-bar
			a.navbar-brand(href="#") pol.bli.sh
				h6.subtitle #{appTitle}
		div.collapse.navbar-collapse#navbar-collapse-1
			
						
			if (currentUser)		
				ul.nav.navbar-nav.navbar-left
					li(class=(menu === 'index')?'active':'')
						a(href='/') 
							h4 Home
					li(class=(menu === 'create')?'active':'')
						a(href="/create") 
							h4 Create
					li(class=(menu === 'admin')?'active':'')
						a(href="/admin") 
							h4 Admin
				ul.nav.navbar-nav.navbar-right
					li
						a(href="/logout") 
							h4 Log out
			else
				ul.nav.navbar-nav.navbar-left
					li(class=(menu === 'index')?'active':'')
						a(href='/') 
							h4 Home
				ul.nav.navbar-nav.navbar-right
					li(class=(menu === 'login')?'active':'')
						a(href='/login') 
							h4 Log in
					li(class=(menu === 'signup')?'active':'')
						a(href='/signup') 
							h4 Sign up